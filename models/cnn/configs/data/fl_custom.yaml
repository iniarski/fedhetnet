# configs/data/fl_custom.yaml
#
# Each client loads its own independent dataset file – realistic silo FL.
# Use: python train_fl.py data=fl_custom
#
# Per-client overrides:
#   class_sampling  – if omitted, the top-level class_sampling is used
#   seq_len         – if omitted, train.seq_len from the main config is used

mode: fl_custom

# Validation data (shared, centralised evaluati

class_sampling: [1, 1, 1, 1, 1, 1, 0] # default; overridable per client

# ── Client definitions ────────────────────────────────────────────────────
clients:
  - id: 0
    path: "/home/filip/projects/fedhetnet/datasets/awid3_ds_split1.sh"
  - id: 1
    path: "/home/filip/projects/fedhetnet/datasets/awid3_ds_split2.sh"
  - id: 2
    path: "/home/filip/projects/fedhetnet/datasets/awid3_ds_split3.sh"

# ── Topology ──────────────────────────────────────────────────────────────
clients_per_round: 2 # clients sampled per round

# ── Schedule ──────────────────────────────────────────────────────────────
n_rounds: 2000
local_epochs: 1 # more local epochs often beneficial for heterogeneous data

# ── Optimisers ────────────────────────────────────────────────────────────
client_lr: 0.01
client_momentum: 0.9
server_lr: 0.001
